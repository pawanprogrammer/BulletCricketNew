<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.trishasofttech.bulletcricketlive">

    <uses-permission android:name="android.permission.INTERNET">

    </uses-permission>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>

    <application
        android:allowBackup="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TestingActivity">

        </activity>
        <activity
            android:name=".Activity.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Fragment.AboutUsFragment"></activity>
        <activity android:name=".Fragment.AdvertiseFragment"></activity>
        <activity android:name=".Fragment.PrivacyFragment"></activity>
        <activity android:name=".livetabs.LiveDetailsActivity"></activity>
        <activity android:name=".Activity.SeriesActivity"></activity>
        <activity android:name=".Activity.DetailsNewsActivity"></activity>
    </application>

</manifest>